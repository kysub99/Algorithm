#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int y;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    cin >> y;
    cout << y - 543;
}
